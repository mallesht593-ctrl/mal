name: Build
on :
push:
  branches: [main]
jobs:
  build:
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v4
      - name: show files
        run: ls -la
      - name: Print branch
        run: echo "Build on ${{ github.ref }}" 
